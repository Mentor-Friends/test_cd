name: Django CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  run_pull:
    runs-on: ubuntu-latest
    steps:
    - name: connect and pull
      run: ssh hukum@secrets.SSH_HOST -p2222 thinkagain@123 "cd test_deploy && git checkout main && git pull && exit"
